#include "bits/stdc++.h"
 
using namespace std; 
 
#define sz(x) int(x.size())
 
int main(void) { 
 ios_base::sync_with_stdio(0); 
 cin.tie(0); 
 cout.tie(0); 
 int N; 
 cin >> N; 
 cout << (N % 2 == 0 ? "YES" : "NO") << '\n'; 
 return 0; 
}