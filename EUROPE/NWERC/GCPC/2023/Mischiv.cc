#include "bits/stdc++.h"

using namespace std; 

int main(void)
{
    int D; 
    cin >> D; 
    if(D >= 10)
    {
        cout << 1 << " " << 2 << " " << 3 << '\n'; 
    }
    else 
    {
        cout << 29 << " " << 53 << " " << 71 << '\n'; 
    }
    return 0; 
}