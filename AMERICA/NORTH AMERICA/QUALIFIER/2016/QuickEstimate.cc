#include "bits/stdc++.h"

using namespace std; 

int main(void) {
    int tc; 
    for(cin >> tc; tc--; ) { 
        string s; 
        cin >> s; 
        cout << s.size() << '\n'; 
    }
    return 0; 
}